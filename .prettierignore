package-lock.json
dist/
node_modules/
.dcp/
coverage/
.gitnexus/
*.min.js
*.min.css

# Syntax-incompatible test fixtures owned by other implementers — prettier
# cannot parse them. They are tracked separately as failures of the respective
# bug-fix groups; until the type-aware syntax is corrected, prettier --check
# would otherwise report a hard error rather than a formatting drift. Remove
# these entries once the upstream test files compile under the prettier parser.
tests/permission-gates.test.ts
tests/source-coverage-meta.test.ts
